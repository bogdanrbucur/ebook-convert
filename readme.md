# eBook conversion script

PowerShell script to convert all `.mobi` files in a given folder to `.epub`. It then appends `.DELETE` to all `.mobi` files.

Requires [Calibre](https://calibre-ebook.com/download) on the machine.
